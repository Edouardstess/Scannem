{
    "name": "edouardstess/scannem",
    "description": "Cartes a QR code signees et verification a l'entree (usage unique)",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "ext-pdo": "*",
        "ext-json": "*",
        "ext-hash": "*",
        "endroid/qr-code": "^5.0"
    },
    "suggest": {
        "ext-gd": "Export des QR en PNG. Sans elle, la planche d'impression fonctionne quand meme : elle utilise du SVG.",
        "ext-redis": "Quota en memoire plutot qu'en base. Inutile en dessous de plusieurs centaines de scans par seconde."
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": {
            "Scannem\\": "src/"
        },
        "files": [
            "src/bootstrap.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Scannem\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "test": "phpunit",
        "serve": "php -S localhost:8000 -t public"
    }
}
